# deps / logs / env
node_modules/
*.tgz
*.log
.env

# ---- build outputs (NAO vai pro github) ----
dist/
native/*/target/
native/laya-serve/ort-musl/*.so
native/laya-serve/ort-musl/*.a

# ---- model weights (baixado via Release/download em runtime) ----
*.onnx
models/model.onnx

# temp / cache
.cache/
tmp/
C:/temp/
*cmp-body.json

# vendored rust patches (gerado local, nao vai pro git)
native/*/vendor/
